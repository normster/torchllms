# Qwen 2.5 instruct template (no reasoning, no structured tool calls).
# For Qwen3 with <think>/<tool_call>/<tool_response>, use qwen3_chatml.yaml
# or qwen3_chatml_nothink.yaml instead.

strip_whitespace: true
strip_reasoning_from_prior_turns: false
inject_empty_reasoning_for_last_turn: false
generation_prompt_suffix: "<|im_start|>assistant\n"
# <|im_end|> and <|endoftext|> both terminate assistant turns.
stop_token_ids: [151645, 151643]

roles:
  system:
    envelope_start: "<|im_start|>system\n"
    envelope_end:   "<|im_end|>\n"

  user:
    envelope_start: "<|im_start|>user\n"
    envelope_end:   "<|im_end|>\n"

  assistant:
    envelope_start: "<|im_start|>assistant\n"
    envelope_end:   "<|im_end|>\n"

  # Qwen 2.5 renders tool messages as role="ipython" (Llama-style).
  tool:
    envelope_start: "<|im_start|>ipython\n"
    envelope_end:   "<|im_end|>\n"
